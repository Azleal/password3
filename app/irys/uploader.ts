import { GateType } from "@/components/Gate/GateSetup";

/**
 * 上传gates信息
 * keys, 用来加密, 长度需要一致, 第一个key为空(// TODO 将来第一个key也可以不为空,随机生成后分享给指定地址)
 * 使用统一的iv, salt
 * */ 
function uploadGates(gates: GateType[], keys: string[]){}

//上传vault中保存的信息
function uploadVaultItem(){

}